<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Reporte de previo</title>
</head>
<body style="margin:0; padding:0; background-color:#f4f7f9; font-family: Arial, Helvetica, sans-serif;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color:#f4f7f9; padding:24px 0;">
    <tr>
      <td align="center">
        <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="max-width:480px; background-color:#ffffff; border-radius:12px; overflow:hidden;">
          <tr>
            <td style="background-color:#41b6c8; padding:20px 24px;">
              <span style="color:#ffffff; font-size:18px; font-weight:bold;">IAL Network</span>
            </td>
          </tr>
          <tr>
            <td style="padding:24px;">
              <p style="margin:0 0 16px; font-size:16px; color:#212529;">
                Se generó un reporte de <strong>previo{{ previo.type == 'Inspección' ? ' / inspección' : '' }}</strong> para el expediente <strong>{{ import.clientReference }}</strong> ({{ import.idCompany.name }}).
              </p>
              <table role="presentation" width="100%" cellpadding="8" cellspacing="0" style="font-size:14px; color:#404040;">
                <tr>
                  <td style="color:#909090;">Tipo</td>
                  <td><strong>{{ previo.type }}</strong></td>
                </tr>
                {% if previo.authority %}
                <tr>
                  <td style="color:#909090;">Autoridad</td>
                  <td><strong>{{ previo.authority }}</strong></td>
                </tr>
                {% endif %}
                <tr>
                  <td style="color:#909090;">Lugar</td>
                  <td><strong>{{ previo.place }}</strong></td>
                </tr>
                <tr>
                  <td style="color:#909090;">Fecha</td>
                  <td><strong>{{ previo.date|date('d/m/Y') }}</strong></td>
                </tr>
              </table>
              <p style="margin:24px 0 0; font-size:13px; color:#909090;">
                El reporte completo va adjunto en PDF a este correo.
              </p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
