security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    # https://symfony.com/doc/current/security.html#hierarchical-roles
    role_hierarchy:
        ROLE_EXECUTIVE: [ROLE_USER]
        ROLE_ADMIN: [ROLE_EXECUTIVE]

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider

            # blocks accounts whose status is not "active" (see App\Security\UserChecker)
            user_checker: App\Security\UserChecker

            # https://symfony.com/doc/current/security.html#form-login
            form_login:
                login_path: login
                check_path: login
                username_parameter: email
                password_parameter: password
                enable_csrf: true
                default_target_path: dashboard

            logout:
                path: logout
                target: index

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # public entry points
        - { path: ^/login$, roles: PUBLIC_ACCESS }
        - { path: ^/register$, roles: PUBLIC_ACCESS }
        - { path: ^/user/new$, roles: PUBLIC_ACCESS }
        # puente temporal sin login para clientes que aun no se cambian al flujo nuevo
        - { path: ^/legacy, roles: PUBLIC_ACCESS }
        # user administration is admin-only (must be listed before the broader /dashboard rule)
        - { path: ^/dashboard/usuarios, roles: ROLE_ADMIN }
        # everything else under /dashboard requires an authenticated, active account
        - { path: ^/dashboard, roles: ROLE_USER }
        # EasyAdmin backend is staff-only
        - { path: ^/admin, roles: ROLE_EXECUTIVE }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
